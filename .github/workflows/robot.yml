name: Robot Tests

on:
  schedule:
    - cron: 41 5 * * TUE
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      version: "1.0.0"

  test:
    needs: build
    uses: ./.github/workflows/test.yml
